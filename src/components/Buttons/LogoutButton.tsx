import { Button } from "../shadcnui/button";

const LogoutButton = () => {
  return (
    <Button
      type="button"
      variant={"destructive"}>
      Logout
    </Button>
  );
};

export default LogoutButton;
